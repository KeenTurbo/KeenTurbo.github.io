{% extends 'base.html.twig' %}

{% block title '编辑回复"' ~ comment.topic.title ~ '"' %}

{% block main %}

    <h5 class="mb-4">编辑回复</h5>

    {{ form_start(form, { 'attr': {'novalidate': 'novalidate'} }) }}
    {{ form_widget(form) }}

    <div class="form-group">
        <button class="btn btn-primary" type="submit">保存</button>
    </div>

    {{ form_end(form) }}

{% endblock %}