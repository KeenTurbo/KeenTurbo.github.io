{{ form_start(form, { method: 'POST', action: path('group_topic_comment_new', {'id': topic.id}), 'attr': {'novalidate': 'novalidate'} }) }}
{{ form_widget(form) }}

<button class="btn btn-primary" type="submit">发表回复</button>
{{ form_end(form) }}