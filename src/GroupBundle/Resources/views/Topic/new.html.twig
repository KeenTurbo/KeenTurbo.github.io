{% extends '::base.html.twig' %}

{% block title '发表新主题' %}

{% block main %}

    <h5 class="mb-4">发表新主题</h5>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}

    <div class="form-group">
        <button type="submit" class="btn btn-primary">发起主题</button>
    </div>

    {{ form_end(form) }}

{% endblock %}